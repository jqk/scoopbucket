{
    "version": "9.7.0",
    "description": "An open-source build automation tool focused on flexibility and performance. (Binaries only without documentation)",
    "homepage": "https://gradle.org",
    "license": "Apache-2.0",
    "suggest": {
        "JDK": "java/openjdk"
    },
    "url": "https://services.gradle.org/distributions/gradle-9.7.0-bin.zip",
    "hash": "84fbba45c7f4c64abc77460e1c00f541e9f960e3c7ed2538f1ede19eacd873ae",
    "extract_dir": "gradle-9.7.0",
    "post_install": [
        "$current_env = [System.Environment]::GetEnvironmentVariable('GRADLE_USER_HOME', 'User')",
        "If ($null -eq $current_env) {",
        "    $path = \"$dir\\.gradle\"",
        "    [System.Environment]::SetEnvironmentVariable('GRADLE_USER_HOME', $path, 'User')",
        "    $Env:GRADLE_USER_HOME = $path",
        "    Write-Host \"Environment variable 'GRADLE_USER_HOME' set to '$path'\"",
        "}"
    ],
    "persist": [
        ".gradle",
        "caches"
    ],
    "bin": "bin\\gradle.bat",
    "checkver": {
        "url": "https://gradle.org/releases/",
        "regex": ">([\\d.]+)</a>"
    },
    "autoupdate": {
        "url": "https://services.gradle.org/distributions/gradle-$version-bin.zip",
        "hash": {
            "url": "$url.sha256"
        },
        "extract_dir": "gradle-$version"
    }
}
